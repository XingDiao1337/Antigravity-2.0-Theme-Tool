# YUZAKI · Antigravity 背景美化 —— 玩法全攻略

> 工具：`Antigravity-Background.bat`（单文件）｜ 版本：v4.5.9

---

## 一、内置功能（安装即生效）

| 功能 | 说明 | 参数 |
|---|---|---|
| 壁纸替换 | 修复"壁纸闪现后消失"bug，壁纸永久显示 | 安装时选图 |
| 透明度（遮罩） | 壁纸压暗层 0~1，推荐 0.2~0.35 | 安装时输入 |
| 背景模糊 | 壁纸高斯模糊 0~50px | 安装时输入 |
| Sidebar 毛玻璃 | 半透明深色底 + 高模糊 | 自动 = 背景模糊+18px（上限 60） |
| 分层毛玻璃（UI 全面重写） | 卡片/次级/浮层/输入框各自模糊度，全部联动 | 内置常量 |
| 浅色/深色自动适配 | 跟随应用主题切换 | 自动 |
| 动画 | 壁纸淡入、弹窗 pop、悬停动效、过渡 | 内置 |
| 滚动条/选区 | 半透明圆角细条 + 蓝色高亮 | 内置 |
| 隐藏顶部菜单栏 | Antigravity/File/View/Window 按钮可隐藏 | 安装时选择 |
| 隐藏 Install IDE 按钮 | 无条件隐藏 | 内置 |
| 原生标题栏按钮配色 | 采样壁纸右上角色调替代纯黑 | 自动 |
| custom.css | 自定义样式注入（优先级最高） | 自行编辑 |

## 二、模糊分层体系（联动背景模糊）

| 层级 | 控件 | 模糊规则 |
|---|---|---|
| L0 | 壁纸层 | 背景模糊（用户设置） |
| L1 | 主背景 | 透明直透 |
| L2 | 侧栏 | 背景+18（上限 60） |
| L3 | 卡片/设置面板 | 与侧栏一致 |
| L4 | 次级控件 | 侧栏-8（下限 10） |
| L5 | 浮层（dialog/menu/tooltip） | 侧栏+10（上限 70） |
| L6 | 输入框 | 侧栏-14（下限 8） |
| 菜单 | 顶部应用菜单 | 高不透明 0.92 + 模糊 34 + z 99999 |
| 下拉列表 | role=listbox | z 100000（portal 置顶，容器 z-[6000] 提升 99998） |

- 性能优化：仅最外层容器保留 backdrop-filter，嵌套禁用（共 5 个模糊层）。
- 设置窗口：窗口背景 34px + 导航 24px + 设置项 18px 分层磨砂。

## 三、工作原理

1. **解包**：asar extract 到临时目录。
2. **注入**：生成 dist/backgroundInjection.js（CSS + 运行时钩子），修补 dist/utils.js：
   - 加 require("./backgroundInjection")
   - 在 win.loadURL(url) 前加 installBackground(win)
3. **运行时**：页面加载事件（did-finish-load 等）+ 定时器执行注入脚本，追加 style 元素。
4. **CSS 要点**：
   - body::before（z-index -1）壁纸层，body::after 遮罩层
   - --color-background 强制透明（修复闪退根因）
   - 变量覆盖 + 类选择器双层实现毛玻璃
5. **安全**：一次性原版备份 .bak、临时文件打包、原子替换 + 失败回滚、5 项验证。

## 四、文件说明

| 文件 | 说明 |
|---|---|
| Antigravity-Background.bat | 唯一工具：安装/卸载/选图/透明度/模糊度/隐藏菜单 |
| background.png | 默认壁纸 |
| custom.css | 自定义样式（安装时打进 asar） |
| README.md | GitHub 双语说明 |
| LICENSE | MIT 许可证 |

## 五、常见问题

| 问题 | 解决 |
|---|---|
| 壁纸不显示/闪退 | 重跑安装（自动还原重装） |
| 更新后壁纸没了 | 应用更新覆盖 app.asar，重跑安装 |
| 想恢复原样 | 菜单选 [2] 卸载 |
| 下拉框被遮挡 | v4.5.6+ 已修复（容器 99998 + 列表 100000） |
| 窗口最小化时毛玻璃不渲染 | Chromium 特性，恢复前台即正常 |
